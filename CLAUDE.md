# CLAUDE.md

**Etherscope** — Ethereum trading & on-chain analytics dashboard. Solo/personal tool, cost-sensitive.

## What this project is

Professional dashboard for analyzing ETH market + on-chain data: price/volume, stablecoin flows, exchange flows, whale tracking, user-defined alerts, derivatives, DEX order flow, smart-money leaderboard, mempool whale tracking. v3 (planned) adds DeFi & staking layer.

**Design doc:** `docs/superpowers/specs/2026-04-23-eth-analytics-dashboard-design.md` — authoritative source of truth for scope and architecture. Read it before making non-trivial decisions.

## Stack

- **Backend:** Python 3.12, FastAPI, Pydantic v2, SQLAlchemy, `arq` (Redis queue), `web3.py`, `dune-client`, `httpx`, `pandas`
- **Frontend:** React 18 + Vite + TypeScript, TanStack Query, TradingView Lightweight Charts, Recharts, Tailwind, shadcn/ui
- **Data:** Postgres 16, Redis 7
- **Deploy:** Docker Compose on a single VPS (Hetzner target)

## Data sources

- **Self-hosted Geth + Lighthouse:** primary JSON-RPC + WebSocket source; required for `newPendingTransactions` (mempool) and beacon-chain reads. Configure via `ALCHEMY_WS_URL` (e.g. `ws://172.17.0.1:8546`).
- **Alchemy (free tier):** fallback WS endpoint when no self-hosted node is configured (used in dev / cheap deploys; mempool unavailable on free tier).
- **Dune Analytics:** labeled flow queries (exchange flows, stablecoin flows, DEX data) — ~5-minute freshness
- **CoinGecko + Binance public API:** price / OHLCV
- **Etherscan:** address labels / metadata

Cache aggressively in Redis. Respect free-tier rate limits; schedule syncs accordingly.

## Repository layout (target)

```
backend/
  api/         FastAPI app, routers, schemas
  workers/     arq tasks (price sync, Dune sync, alert eval)
  realtime/    Alchemy WS listener
  core/        shared models, db, config
  tests/
frontend/
  src/
docs/
  superpowers/specs/
docker-compose.yml
```

## Conventions

- Python: `ruff` + `ruff format`, type hints everywhere, `pytest` for tests
- Frontend: ESLint + Prettier, functional components, hooks over classes
- Never commit secrets — use `.env` (gitignored); `.env.example` documents required keys
- Prefer existing patterns in the codebase over introducing new ones
- **shadcn/ui dropdowns**: `frontend/src/components/ui/Select.tsx` wraps `@radix-ui/react-select` in shadcn-style components (`Select`/`SelectTrigger`/`SelectContent`/`SelectItem`) plus a convenience `SimpleSelect` (default export) with the same `(value, onChange, options)` API as the existing `Pill`. Use `SimpleSelect` when an option list is too long for an inline pill row (e.g. the 14-asset filter on `WhaleTransfersPanel`). Class joiner helper at `frontend/src/lib/cn.ts` (`clsx` + `tailwind-merge`). The `tailwindcss-animate` plugin is registered for shadcn's enter/exit animations.

## Auth

Single-account session login (argon2 password, Redis-backed HttpOnly cookies)
gates the dashboard UI and all protected API routes. `/api/health` stays
public. Operator setup: see `docs/auth-setup.md`. Design: see
`docs/superpowers/specs/2026-04-30-login-auth-design.md`.

## Scope discipline

The design doc's v1 scope is fixed. Do **not** implement v2/v3 features (DEX leaderboard, order flow, clustering, derivatives, backtesting) until v1 is shipped and stable. If a change seems to pull in v2+ work, flag it instead of expanding scope.

## Milestone status

- M0 ✅ scaffold (docker compose, schema, health, React/Vite)
- M1 ✅ ETH price & volume (Binance klines sync → `/api/price/candles` → candlestick+volume chart with 1m/5m/15m/1h/4h/1d selector)
- M2 ✅ on-chain flows (3 Dune queries → `/api/flows/{exchange,stablecoins,onchain-volume}` → panels). Requires Dune query IDs in `.env` (see `docs/dune-setup.md`); panels show "no data yet" gracefully when unset. **v4 update (2026-05-04):** `/api/flows/onchain-volume` migrated off Dune — endpoint now rolls up the live `realtime_volume` table (per-minute, populated by the realtime listener) into hourly buckets via SQL `date_trunc`. **`/api/flows/stablecoins` also migrated off Dune** — the realtime listener's `SupplyAggregator` detects Mint (`from=0x0`) and Burn (`to=0x0`) Transfer events on the 15 tracked stables and flushes hourly to the same `stablecoin_flows` table the Dune query used to populate. Both Dune query IDs are preserved in env (`DUNE_QUERY_ID_ONCHAIN_VOLUME`, `DUNE_QUERY_ID_STABLECOIN_SUPPLY`) for rollback but no longer executed; saves ~1,440 Dune executions/month.
- M3 ✅ whale tracking — Alchemy WS listener persists ETH + 16 stables (USDT/USDC/DAI/PYUSD/FDUSD/USDS/GHO/EUROC/ZCHF/EURCV/EURe/tGBP/USDe/XSGD/BRZ/EURS) transfers above threshold to `transfers`; non-USD stables (EUROC, ZCHF) use `price_usd_approx` per token for FX-aware thresholds; `/api/whales/transfers` exposes them with CEX labels; live-refreshing panel. Needs `ALCHEMY_API_KEY`; thresholds via `WHALE_ETH_THRESHOLD` / `WHALE_STABLE_THRESHOLD_USD`. **After deploy:** re-paste `backend/dune/stablecoin_supply.sql` into the existing `DUNE_QUERY_ID_STABLECOIN_SUPPLY` query on Dune and click Save so the supply panel covers the new stables.
- M4 ✅ alerts engine — arq cron `evaluate_alerts` every minute; 7 rule types (price above/below/change%, whale transfer, whale→exchange, exchange netflow, **wallet_score_move**); Telegram + HMAC-signed webhook delivery; `/api/alerts/{rules,events}` CRUD; tabbed dashboard panel (Events / Rules) with form-based create/edit + toast on fire. See `docs/telegram-setup.md`. **wallet_score_move (2026-05-07):** fires on whale-grade transfers where at least one party clears `wallet_score.score >= min_score` (default $100k, mirrors `SMART_FLOOR_USD`). Params: `{asset, min_usd, min_score, direction: any|from|to}`. Per-transfer dedup (same as whale_transfer); payload includes `from_score`/`to_score`/`match_side` so Telegram + webhook recipients see why it fired. Builds directly on the v4-wallet-perf-scoring `wallet_score` table — no new schema, just a new SQL filter sharing the `IN (SELECT wallet WHERE score >= floor)` shape with `/api/whales/transfers?smart_only=true`.
- M5 ✅ network activity + polish — realtime listener writes per-block gas/base-fee/tx-count to `network_activity`; `/api/network/{summary,series}` endpoints; dashboard panel with gas + tx-count charts; `/api/health` reports per-source freshness; Topbar dropdown shows data lag per source; ErrorBoundary wraps every panel.

**v1 complete.**

## v2 status

- v2-derivatives ✅ OI + funding rates for ETH perp across Binance/Bybit/OKX/Deribit.
- v2-liquidations ✅ Perp liquidations heatmap — Binance USD-M Futures public WebSocket forceOrder stream (`!forceOrder@arr`, free, no auth) runs as a sibling task inside the realtime container; persists every ETH-USD liquidation event to `perp_liquidation` (one row per event, `ts`/`venue`/`side`(long|short)/`price`/`qty`/`notional_usd`). `/api/derivatives/liquidations?hours=N` returns hourly buckets + a 24h summary tile. `LiquidationsPanel` renders signed-stacked bars (longs above zero, red; shorts below zero, green) plus a long-liquidated/short-liquidated/skew-and-largest tile row. Sits on the Markets page beside the existing Derivatives panel. Single venue (Binance) for v1; the schema's `venue` column allows Bybit/OKX/Deribit to slot in without API change. Listener owns its own reconnect loop so a Binance outage can't disrupt on-chain processing.
- v2-volume-structure ✅ Hourly retail/mid/large/whale buckets of WETH DEX volume, persists to `volume_buckets`. **v4 migration (2026-05-04):** off Dune. The realtime listener's `VolumeBucketAggregator` consumes the same SwapEvent stream the order-flow aggregator does — converts each swap's WETH amount to USD via the block's ETH price, buckets by USD threshold (<$10k retail / <$100k mid / <$1M large / ≥$1M whale), accumulates per (ts_bucket, bucket) with additive on_conflict. The Dune `sync_volume_buckets` cron is commented out in `arq_settings.py` (kept for rollback). Saves ~90 Dune executions/month.
- v2-order-flow ✅ WETH buy vs sell pressure across major DEXes, persists hourly to `order_flow`; `/api/flows/order-flow` endpoint; dashboard panel with buy/sell/net tiles + signed-stacked bar + net line, plus a per-DEX breakdown table (Uniswap V2/V3, Curve, Balancer, Other). **v4 migration (2026-05-04):** all four named DEX rows (Uniswap V2/V3, Curve, Balancer) now flow from the realtime listener. `OrderFlowAggregator` decodes Swap events from a curated 11-entry registry (3 V2 + 5 V3 + 2 Curve TriCrypto pools + the single Balancer V2 Vault) and flushes hourly with additive on_conflict semantics. Per-DEX decoders: V2 reads four uint256s in `data`; V3 reads signed int256 deltas (two's-complement); Curve TokenExchange reads sold_id/bought_id and matches against each pool's curated WETH index; Balancer Vault Swap reads tokenIn/tokenOut topics and filters to WETH-side trades client-side. The Dune cron's `upsert_order_flow` now skips all four named DEX values; only the long-tail 'other' bucket (Sushi, Pancake, Maverick, etc.) still comes from Dune. SQL at `backend/dune/order_flow.sql` retained for the 'other' slice.
- v2-smart-money-leaderboard ✅ Daily refresh of top 50 ETH DEX traders by 30d realized PnL on WETH; persists snapshot per run to `smart_money_leaderboard`; `/api/leaderboard/smart-money` endpoint; dashboard panel. **v5 update (2026-05-06):** migrated off Dune. The cron now reads top-N from the in-house `wallet_score` table (which the daily `score_wallets` cron populates from `dex_swap`), aggregates `weth_bought` / `weth_sold` from `dex_swap` over the same 30d window, joins `address_label` for human-readable names, and persists exactly the same snapshot shape. Tradeoff vs. the Dune feed: only the curated 11-pool DEX set is captured (Uniswap V2/V3 + Curve TriCrypto + Balancer V2 Vault), so wallets that trade primarily on Sushi/Maverick/Pancake won't appear. The Dune query ID is preserved in `DUNE_QUERY_ID_SMART_MONEY_LEADERBOARD` for rollback but no longer executed; saves ~30 Dune executions/month.
- v2-mempool ✅ Self-hosted Geth + Lighthouse node; `backend/app/realtime/mempool.py` subscribes to `newPendingTransactions` concurrently with `newHeads`; whale-sized pending txs persist to `pending_transfers` (auto-cleaned at 30m or on confirm); `/api/whales/pending` endpoint; "Pending" section atop `WhaleTransfersPanel`. Spec: `docs/superpowers/specs/2026-04-28-mempool-tracking-design.md`.
- v2-volume-structure ✅ Hourly Dune refresh bucketing ETH DEX volume into retail (<$10k) / mid ($10k–100k) / large ($100k–1M) / whale (≥$1M); persists to `volume_buckets`; `/api/flows/volume-buckets` endpoint; `VolumeStructurePanel` with USD/% mode toggle. Requires `DUNE_QUERY_ID_VOLUME_BUCKETS` in `.env` (SQL at `backend/dune/volume_buckets.sql`).
- v2-wallet-clustering ✅ On-demand wallet drawer (Etherscan-backed, sync, 7d Postgres `wallet_clusters` cache); shared gas-funder + same-CEX-deposit heuristics with public-funder denylist (CEX hot wallets, Tornado, bridges) suppressing false positives; clickable addresses across whale + smart-money panels via shared `<AddressLink>` + Zustand drawer state; daily 03:11 UTC purge cron drops rows past the 7-day grace window. Requires `ETHERSCAN_API_KEY` in `.env`. Spec: `docs/superpowers/specs/2026-05-01-wallet-clustering-design.md`.
- v2-wallet-profile ✅ Wallet drawer now renders a full profile in one fetch: 30d ETH balance area chart (`wallet_balance_history` table populated lazily via JSON-RPC `eth_getBalance` on the self-hosted Geth archive node, ~30 calls in batches of 5 on first lookup; today's row recomputed each fetch), 7d net-flow bar chart from existing `transfers`, top 5 counterparties (30d), last 15 whale moves involving the address, plus the existing cluster + linked-wallets sections. New endpoint `GET /api/wallets/{address}/profile`. Configure `ALCHEMY_HTTP_URL=http://172.17.0.1:8545` in `.env` (or omit — falls back to Alchemy if `ALCHEMY_API_KEY` is set, or returns the profile without balance data if neither is configured). **Top 8 ERC-20 holdings** by USD value are also surfaced via a curated 23-token list (stables + majors + LSDs + DeFi blue-chips + L2 governance + memes); a single JSON-RPC batch fetches all `balanceOf` calls and CoinGecko `simple/token_price/ethereum` supplies USD pricing (cached 5min in Redis; per-address result cached 60s). Dust below $1 is filtered. **Smart-money tile (2026-05-07):** when the daily scoring cron has produced a `wallet_score` row for the target address, `/api/wallets/{address}/profile` now carries it on `wallet_score` (score / realized_pnl_30d / win_rate_30d / trades_30d / volume_usd_30d / updated_at) and the drawer renders a header tile between the balance card and token holdings — emerald tier ≥ $100k, gold tier ≥ $1M, greyed when scored but below floor. Linked-wallet rows (cluster section) also gain a `score` field and decorate inline with the same ★ badge so smart-money peers stand out without a second request. Single batched IN-clause query covers target + all linked addrs.

**v2 complete.**

## v3 status

- v3-staking ✅ ETH staking flows — `/api/staking/flows` reads from `staking_flows`; `/api/staking/summary` reads validator-set summary from Lighthouse; `StakingFlowsPanel` renders divergent leg bars + sparklines + active-validator-count tile + **total ETH staked tile** (sum of active validator balances at head). Both tiles populate when `BEACON_HTTP_URL` is set. **v4 update (2026-05-04):** the `staking_flows` table is now populated by `sync_beacon_flows` (every 5 min, walks newly-finalized slots from Lighthouse, sums deposits + withdrawals, classifies withdrawals as partial/full by amount threshold, upserts hourly rollups). The Dune query is preserved in `DUNE_QUERY_ID_STAKING_FLOWS` for rollback but no longer executed. `BEACON_HTTP_URL=http://172.17.0.1:5052` is now REQUIRED (not just for tiles — also for the new live cron). Saves ~720 Dune executions/month. Spec: `docs/superpowers/specs/2026-05-02-eth-staking-flows-design.md`.
- v3-staking-by-entity ✅ Per-issuer staking breakdown. **v4 update (2026-05-04):** migrated off Dune. `sync_beacon_flows` now does a second fan-out per slot — each deposit's withdrawal-credentials address (last 20 bytes of type-01 creds) and each withdrawal's `address` field are joined against the curated `address_label` registry (categories: staking + lrt) to derive an issuer name. Labels collapse to top-level entities (e.g. "Lido stETH" + "Lido withdrawal queue" → "Lido"). Unknown addresses bucket as "Unattributed". Same `staking_flows_by_entity` table, same `/api/staking/flows/by-entity` endpoint, same panel. The Dune query is preserved in `DUNE_QUERY_ID_STAKING_FLOWS_BY_ENTITY` for rollback. Saves ~720 Dune executions/month.
- v3-bridge-flows ✅ L1↔L2 bridge flows — Dune `bridge_flows.sql` filters `tokens.transfers` to/from the canonical L1 bridge contracts for Arbitrum / Base / Optimism / zkSync; persists per-(hour, bridge, direction, asset) USD volume to `bridge_flows`. `/api/flows/bridge` endpoint; `BridgeFlowsPanel` renders divergent bars + sparklines per L2 (deposit leg right, withdrawal leg left). New "Bridges (L1↔L2)" section on the Onchain page. Requires `DUNE_QUERY_ID_BRIDGE_FLOWS` in `.env` (SQL at `backend/dune/bridge_flows.sql`).
- v3-lst ✅ LST market share — hourly arq cron (`sync_lst_supply`, minute 7) batches `eth_call(totalSupply())` + per-token exchange-rate reads against the self-hosted Geth node for stETH/rETH/cbETH/sfrxETH/mETH/swETH/ETHx, computes ETH-equivalent supply per token, persists both raw `supply` and `eth_supply` to `lst_supply` (eth_supply nullable so a single bad rate selector falls back to raw rather than rendering nothing); `/api/staking/lst-supply` endpoint returns both; `LstMarketSharePanel` renders Recharts stacked area + per-token current % share using `eth_supply ?? supply`. Reuses `ALCHEMY_HTTP_URL`. Rate ABIs: rETH/ETHx use `getExchangeRate()`, cbETH `exchangeRate()`, sfrxETH `pricePerShare()` (ERC4626), swETH `swETHToETHRate()`, mETH `mETHToETH(1e18)` on the Mantle staking pool, stETH skipped (rebasing). Spec: `docs/superpowers/specs/2026-05-02-lst-market-share-design.md`.
- v3-defi-tvl ✅ DeFi protocol TVL — hourly arq cron (`sync_defi_tvl`, minute 17) hits DefiLlama's free public API for 9 protocols (Aave v3, Sky-Lending, Morpho-Blue, Compound v2/v3, Sparklend, Lido, EigenLayer, Pendle); persists per-asset Ethereum-mainnet locked balances to `protocol_tvl`; `/api/defi/tvl/latest` endpoint pre-aggregates per-protocol totals; `DefiTvlPanel` renders a protocol picker (shadcn Select) + per-asset horizontal bar (top 12 assets, "+N more" overflow). No new env var (DefiLlama is unauthenticated). v1 displays raw DefiLlama snapshot, daily granularity. Uniswap v3 omitted — DefiLlama doesn't expose per-asset breakdown for DEX LPs. Spec: `docs/superpowers/specs/2026-05-02-defi-protocol-tvl-design.md`.
- v3-dex-pool-tvl ✅ DEX pool TVL — hourly arq cron (`sync_dex_pool_tvl`, minute 27) hits DefiLlama's `/yields/pools` endpoint, filters Ethereum mainnet + Uniswap V2/V3 + Curve + Balancer V2, persists top-100 pools by TVL to `dex_pool_tvl`; `/api/defi/dex-pools/latest` endpoint returns the latest snapshot sorted desc; `DexPoolTvlPanel` renders a DEX picker (shadcn Select) + top-20 pool list with horizontal bars. Companion to v3-defi-tvl which covers lending-style protocols (DefiLlama exposes per-asset for lending vaults but per-pool for AMMs — different shape, different endpoint). No new env var. Spec: `docs/superpowers/specs/2026-05-02-dex-pool-tvl-design.md`.
- v3-live-volume ✅ Per-minute on-chain volume — `MinuteAggregator` runs in-process inside the realtime listener, accumulates every Stable Transfer log (threshold-free) into a per-(minute, asset) bucket, flushes to `realtime_volume` table on minute-rollover; `/api/volume/realtime?minutes=N` endpoint; `LiveVolumePanel` renders a Recharts stacked area + per-asset legend with most-recent-minute USD/min. ~5s refetch. No new env var (reuses existing realtime listener). Spec: `docs/superpowers/specs/2026-05-02-live-volume-design.md`.
- v4-wallet-perf-scoring ✅ Per-wallet performance metrics — realtime listener captures every decoded Swap event into `dex_swap` (with originating EOA looked up from the block's transaction list), daily cron at 04:13 UTC walks the last 30d, runs FIFO PnL + win-rate + volume rank per wallet, upserts into `wallet_score` (latest-only). FIFO matcher in `app/services/wallet_scoring.py` is a pure compute kernel with 6 unit tests covering profitable/losing round-trips, partial fills across cost-basis lots, orphan sells (no inventory), and win-rate sample-size minimums. v1 score = realized_pnl_30d directly; future revisions blend in win_rate × log(volume). **Wallet-importance-weighting (2026-05-07):** `WhaleTransfersPanel` now renders a `★ Smart only` toggle chip (URL-persisted via `?whaleSmart=1`) that flips `/api/whales/transfers` to a SQL-level filter — backend joins `wallet_score` and keeps only transfers where either party clears the $100k 30d-PnL floor (constant `SMART_FLOOR_USD` mirrored backend ↔ frontend). When the toggle is on, rows sort by `max(from_score, to_score)` desc so the highest-PnL wallet's move surfaces first; when off, the chrono order is preserved and the subtitle reports the smart-money row count. Limit auto-bumps from 100→500 in smart-only mode so the filtered subset isn't bottle-necked. **Smart-money net-direction tile (2026-05-08):** new Overview panel `/api/smart-money/direction` — aggregates the last 24h of `dex_swap` rows where the EOA's wallet_score clears the same $100k floor, sums WETH bought vs sold in USD, and returns a 24h headline + 7-day daily sparkline (zero-filled to 7 buckets so the bar shape stays stable on quiet days). Net positive = smart-money cohort accumulating ETH on-chain, negative = distributing. Backend caches 5 min in Redis (`smart_money_direction:current`); panel refetches every 60s. `SmartMoneyDirectionPanel` ships with the headline number tinted up/down, a buy/sell tile pair underneath, signed-bar Recharts sparkline, and an active-wallet count. Slotted second on the default Overview layout (after market-regime, before cex-net-flow). The smart-money signal now spans **four** surfaces: panel filter ✓ alert rule ✓ drawer tile ✓ overview tile.
- v3-staking-yields ✅ APR overlay on LST + LRT panels — hourly arq cron (`sync_staking_yields`, minute 47) hits DefiLlama's `/yields/pools` once, filters to a curated `(project, symbol)` map per LST symbol / LRT slug, upserts a single row per `(kind, key)` into `staking_yield`. `/api/staking/yields` returns `{lst: {symbol: apy}, lrt: {slug: apy}, updated_at}`. Both panels render APR % beside each row's existing supply / share %; missing pools (e.g. Mantle Restaking has none on DefiLlama) display "—". No new env var (DefiLlama unauthenticated). Cron rotation now: defi_tvl=17, dex_pool=27, lrt_tvl=37, staking_yields=47 — four DefiLlama crons evenly staggered every 10 min.
- v3-lrt-tvl ✅ Liquid-restaking token issuers — hourly arq cron (`sync_lrt_tvl`, minute 37) hits DefiLlama's `/protocol/{slug}` for the curated 6 LRT issuers (ether.fi, Renzo, Kelp, Puffer, Swell, Eigenpie); aggregates each protocol's per-asset breakdown into a single USD total; persists to `lrt_tvl` table; `/api/restaking/lrt-tvl/latest` endpoint; `LrtTvlPanel` renders a horizontal-bar list with per-issuer share % and a total tile. Sits in the Staking section beside the LST panel. No new env var (DefiLlama unauthenticated). EigenLayer itself stays in the DeFi-TVL panel as the underlying restaking layer.
- v4-market-regime ✅ Final v4 vision card. Single Overview tile labelling the current market regime — accumulation / distribution / euphoria / capitulation / neutral — with a confidence % and a transparent six-feature breakdown. Rule-based (deterministic, no ML), six features each z-scored vs a 30-day baseline and weighted into a signed score: CEX net flow (0.25), funding rate (0.20), OI 24h Δ (0.10), staking net flow (0.10), smart-money direction (0.20), whale-volume share (0.15). Threshold table in `app/services/regime.py` maps the signed total to a label; weights and thresholds are named constants for one-line tuning after a week of live observation. No new tables — `/api/regime` reads from `transfers.flow_kind`, `derivatives_snapshots`, `staking_flows`, `dex_swap` × top-100 `wallet_score`, `volume_buckets`. Response cached 60s in Redis. `MarketRegimePanel` shows the label + score + per-feature signed bars (positive = bearish bias, negative = bullish). Spec: `docs/superpowers/specs/2026-05-04-market-regime-classifier-design.md`.

## v5 status

- v5-onchain-perps ✅ GMX V2 on-chain perp tracking on Arbitrum. New sibling Docker service `arbitrum_realtime` (`backend/app/realtime/arbitrum_listener.py`) subscribes to `newHeads` on Arbitrum and per-block fetches GMX V2 EventEmitter logs (`0xC8ee91A...`) via `eth_getLogs` with topics[1] OR-filtered to PositionIncrease + PositionDecrease. Decoder (`app/realtime/gmx_v2_decoder.py`) walks the `EventLogData` struct via eth-abi (added as a dep — the struct is too nested to hand-roll safely) and classifies five `event_kind` values: open / increase / close / decrease / liquidation. Liquidations are PositionDecrease with `orderType==7`. Originating EOA resolved per tx via Redis-cached (`arb_tx_from:` keys, 1h TTL) `eth_getTransactionByHash` — GMX's `msgSender` is the router, not the trader. One new table `onchain_perp_event` (event-sourced, ON CONFLICT DO NOTHING on `(tx_hash, log_index)`); open positions reconstructed on read by latest-event-per-(account, market, side) windowed over the last 30d. Three endpoints under `/api/perps`: `events` (filterable feed), `summary` (24h tile row + reconstructed open-size skew), `largest-positions` (top-N currently open). Frontend `OnchainPerpsPanel` (Markets page, default width 3) ships three tabs (Events / Liquidations / Open positions) with full wallet-drawer integration via `<AddressLink>`. Endpoints: `ARBITRUM_WS_URL` + `ARBITRUM_HTTP_URL`, both fall back to `ALCHEMY_API_KEY` (Alchemy serves Arbitrum on the same key/plan), idle-mode safe when unset. v1 covers 8 markets (ETH/BTC/SOL/AVAX/ARB/LINK/DOGE/NEAR-USD). Forward-only; no historical backfill in v1. Spec: `docs/superpowers/specs/2026-05-06-gmx-v2-perps-design.md`. Setup: `docs/arbitrum-setup.md`.

## UI polish

- Live chart ✅ Direct browser-to-Binance WebSocket (combined `@trade` + `@kline_<tf>` streams) drives the price-hero ticker and in-place candle updates via `series.update()`; backend wraps `/api/price/candles` in a 60s Redis cache for the bootstrap path. Spec: `docs/superpowers/specs/2026-05-01-live-chart-ws-design.md`.
- Customizable overview ✅ React Router 4-page split (`Overview · Markets · Onchain · Mempool`); overview supports drag-to-reorder, add/remove, and bento-grid resize (S/M/L/Full → 1/2/3/4 cols) via `dnd-kit/sortable` + a 4-col CSS grid, persisted to LocalStorage (schema v2); category pages are fixed-in-code, derived from a single `lib/panelRegistry.ts`. Desktop only (`≥md`); mobile renders a clean default stack. Specs: `docs/superpowers/specs/2026-05-01-customizable-layout-design.md`, `docs/superpowers/specs/2026-05-01-bento-grid-resize-design.md`.
- Panel-responsive content ✅ `@tailwindcss/container-queries` plugin + `<PanelShell>` wraps every panel in an `@container` div so inner Tailwind classes (`@xs:` / `@sm:` / `@md:` / `@2xl:`) react to the panel's own rendered width rather than the viewport. v1 ships narrow-mode passes for the 5 most pinch-sensitive panels (WhaleTransfers, SmartMoneyLeaderboard, AlertEvents, NetworkActivity, PriceHero); other 8 get the foundation only. Spec: `docs/superpowers/specs/2026-05-01-panel-responsive-design.md`.

## Backlog (post-vacation)

Items the operator surfaced in chat that aren't shipped. Not actively scoped — revisit when the operator returns and confirms direction. Order is **rough priority**, not strict.

- ✅ **On-chain perps (GMX V2)** — shipped 2026-05-06 as v5-onchain-perps; see status block above. Vertex / Aevo / dYdX still deferred — the Arbitrum listener scaffolding is now in place so adding them is decoder + market-registry work, no infrastructure rebuild.
- ✅ **EURS** (Stasis Euro, 2026-05-06) — added to the 16-stable tracked set. Contract `0xdB25f211AB05b1c97D595516F45794528a807ad8`, 2 decimals (unusual; the only sub-6-decimal stable in the registry). Pegged EUR, `price_usd_approx` 1.08. Threaded through `STABLES_BY_ADDRESS`, `WhaleAsset` Literal (backend + frontend), peg map, asset-color palette (sky-400), filter dropdown, and Dune supply SQL (rollback parity, query no longer executed in v4). After next deploy, supply rollups will start populating automatically via the realtime `SupplyAggregator`.
- ✅ **Mantle Network DEX flows** — shipped 2026-05-10. Sibling listener `mantle_realtime` (profile-gated, opt-in) consumes Agni V3 Swap events for the top-5 MNT pools, persists hourly buy/sell pressure to `mantle_order_flow`. New `/api/flows/mantle-order-flow` + `MantleOrderFlowPanel` on Markets page. Public Mantle WS via `MANTLE_WS_URL` (idle when unset). USD valuation at read time via Redis-cached CoinGecko MNT/USD — writer is price-independent so a CoinGecko outage cannot drop swap data. v1 is Agni-only; broader DEX coverage (FusionX, Cleopatra, Butter) is registry-only follow-up. Spec: `docs/superpowers/specs/2026-05-10-mantle-dex-flows-design.md`. Setup: `docs/mantle-setup.md`.
- 🟡 **Browser Binance WS proxy** — surfaced 2026-05-10 when the live-chart WS to `wss://stream.binance.com:9443` failed on the dev wifi. Same ISP DNS hijack documented in the Environment note, but the docker-compose `dns:` override only fixes container resolution — the browser-originating WS in `frontend/src/lib/binanceWS.ts:32` still hits the host resolver and gets a private IP with a fake "blocking-page-authority" cert. Workaround for now: point the dev Mac's system DNS at 1.1.1.1/8.8.8.8 (System Settings → Network → Wi-Fi → Details → DNS). Proper fix: add a FastAPI WebSocket endpoint (e.g. `/ws/binance`) that opens an upstream WS to Binance from the API container and pipes frames bidirectionally, then have `binanceWS.ts` connect to `ws://localhost:8000/ws/binance?streams=…`. Removes host-DNS dependency permanently and survives any future ISP block.
- 🔴 **Hyperliquid in/out tracking** — **shelved** by operator (2026-05-04). The v4 vision doc card 6 is parked: HL's bridge runs on Arbitrum, mainnet alone can't see HL deposits with usable precision, and the operator declined the proxy-signal v1. If revived, see deleted spec history or path-(b) framing in the v4 vision doc.

## Environment note

Local ISP (dev machine) DNS-intercepts `api.binance.com` and other crypto-exchange domains, returning a fake "blocking-page-authority" cert. `docker-compose.yml` works around this by pinning container DNS to 1.1.1.1/8.8.8.8. In production (Hetzner/Railway) this override is harmless; remove it only if resolver performance matters. **Note:** the override only fixes *container* DNS — browser-side WS connections (e.g. the live-chart `wss://stream.binance.com:9443`) still use the host resolver, so the dev Mac's system DNS must also be pointed at 1.1.1.1/8.8.8.8, or use the WS proxy described in the Backlog above.

## Commands

- `make up` — start the full stack (postgres, redis, api, worker, realtime, frontend)
- `make down` — stop stack
- `make logs` — tail logs
- `make migrate` — run alembic migrations inside the api container
- `make backend-test` — run pytest (uses testcontainers for DB)
- `make frontend-build` — production build of the frontend
- `make lint` — ruff + eslint

### Local development notes

- Backend Python 3.12 venv at `backend/.venv` (managed via `uv`). Activate with `source backend/.venv/bin/activate` or use `.venv/bin/python` directly.
- Frontend Node 20; deps installed under `frontend/node_modules`.
- **Adding a frontend npm package:** `docker-compose.yml` uses an anonymous volume for `/app/node_modules` so host bind-mount perf is decent on macOS. That means a host-side `npm install` doesn't propagate into the running frontend container. After `npm install <pkg>` on the host, also run `docker compose exec frontend npm install <pkg>` (or `docker compose up -d --build frontend` to rebuild from scratch).
- Host: `http://localhost:8000/api/health` (API), `http://localhost:5173` (frontend).
